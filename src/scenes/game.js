import Phaser from "phaser";

class Game extends Phaser.Scene {
  constructor() {
    super("game");
  }

  create() {
    // this.add.image(0, 0, "imgBG").setOrigin(0);
  }

  update() {}
}

export default Game;
